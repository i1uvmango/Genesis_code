# MLP 기반 물리 파라미터 학습 시스템

## 📋 개요

`car_physics_learning_simple.py`는 MLP (Multi-Layer Perceptron)를 사용하여 자동차 시뮬레이션의 물리 파라미터를 학습하는 시스템입니다. CSV 파일에 저장된 실제 차량 움직임 데이터를 사용하여 Genesis 물리 시뮬레이터의 파라미터를 최적화합니다.

## 🎯 목적

CSV 데이터에서 수집한 실제 차량의 움직임 데이터를 기반으로:
- 마찰계수 (friction)
- 차체 질량 (car_mass)
- 바퀴 질량 (wheel_mass)
- 구동 PD 게인 (kp_drive, kv_drive)
- 조향 PD 게인 (kp_steer, kv_steer)

등의 물리 파라미터를 MLP 모델을 통해 학습하고 최적화합니다.

## 🏗️ 아키텍처

### 1. 주요 컴포넌트

#### 1.1 `PhysicsParameterMLP` 클래스
- **역할**: 시뮬레이션 상태를 입력으로 받아 물리 파라미터를 출력하는 신경망 모델
- **입력**: 시뮬레이션 상태 (시간, 위치, 속도 등) - 7차원
- **출력**: 물리 파라미터 7개
  - `friction`: 마찰계수 (0~2)
  - `car_mass`: 차체 질량 (>500)
  - `wheel_mass`: 바퀴 질량 (>10)
  - `kp_drive`: 구동 위치 게인 (>100)
  - `kv_drive`: 구동 속도 게인 (>10)
  - `kp_steer`: 조향 위치 게인 (>500)
  - `kv_steer`: 조향 속도 게인 (>50)
- **구조**: 
  - 입력 레이어: 7차원
  - 히든 레이어: [128, 64, 32] (ReLU + Dropout)
  - 출력 레이어: 7차원 (활성화 함수로 범위 제한)

#### 1.2 `CarSimulationWrapper` 클래스
- **역할**: Genesis 시뮬레이터를 래핑하여 물리 파라미터를 적용하고 시뮬레이션을 실행
- **주요 기능**:
  - 씬 초기화 및 관리
  - 조인트 인덱스 탐색
  - Body 링크 찾기
  - 물리 파라미터 적용 (PD 게인 설정)
  - 시뮬레이션 실행 및 상태 수집

#### 1.3 `load_csv_data()` 함수
- **역할**: CSV 파일에서 차량 움직임 데이터 로드
- **입력**: CSV 파일 경로
- **출력**: pandas DataFrame
- **데이터 구조**:
  - `car_x`, `car_y`, `car_z`: 차체 위치
  - `car_vx`, `car_vy`, `car_vz`: 차체 속도
  - `fl_x`, `fl_y`, `fl_z`, `fl_vx`, `fl_vy`, `fl_vz`: 앞왼쪽 바퀴 상태
  - `fr_x`, `fr_y`, `fr_z`, `fr_vx`, `fr_vy`, `fr_vz`: 앞오른쪽 바퀴 상태
  - `rl_x`, `rl_y`, `rl_z`, `rl_vx`, `rl_vy`, `rl_vz`: 뒤왼쪽 바퀴 상태
  - `rr_x`, `rr_y`, `rr_z`, `rr_vx`, `rr_vy`, `rr_vz`: 뒤오른쪽 바퀴 상태

#### 1.4 `compute_loss()` 함수
- **역할**: 시뮬레이션 결과와 타겟 데이터를 비교하여 손실 계산
- **손실 함수**: 
  - 위치 손실 (MSE): `mean((sim_pos - target_pos)²)`
  - 속도 손실 (MSE): `mean((sim_vel - target_vel)²)`
  - 총 손실: `pos_loss + 0.1 * vel_loss` (위치에 더 큰 가중치)

#### 1.5 `train_with_mlp()` 함수
- **역할**: MLP 모델을 사용하여 물리 파라미터 학습
- **학습 방법**:
  - 유한 차분법 (Finite Difference Method)으로 그래디언트 계산
  - Adam 옵티마이저로 파라미터 업데이트
  - 5 에폭마다 그래디언트 계산 (비용 절감)

## 🔄 학습 프로세스

### 1. 데이터 준비
```
CSV 파일 로드 → pandas DataFrame → 필요한 컬럼 추출 → numpy 배열 변환
```

### 2. 입력 상태 생성
```
시간 정규화 + 이전 프레임의 위치/속도 → 입력 상태 텐서 (7차원)
```

### 3. 파라미터 예측
```
MLP 모델 (입력 상태) → 물리 파라미터 (7차원)
```

### 4. 시뮬레이션 실행
```
물리 파라미터 적용 → Genesis 시뮬레이션 실행 → 차체 상태 수집
```

### 5. 손실 계산
```
시뮬레이션 결과 vs 타겟 데이터 → MSE 손실
```

### 6. 그래디언트 계산 (유한 차분법)
```
파라미터 ± epsilon → 시뮬레이션 실행 → 손실 계산 → 그래디언트 추정
```

### 7. 모델 업데이트
```
그래디언트 역전파 → Adam 옵티마이저 → 파라미터 업데이트
```

## 📊 학습 파라미터

### MLP 모델 하이퍼파라미터
- **입력 차원**: 7 (시간, 위치 3D, 속도 3D)
- **히든 레이어**: [128, 64, 32]
- **출력 차원**: 7 (물리 파라미터)
- **Dropout 비율**: 0.1
- **활성화 함수**: ReLU (히든), Sigmoid/Softplus (출력)

### 학습 하이퍼파라미터
- **옵티마이저**: Adam
- **기본 학습률**: 1e-3
- **에폭 수**: 20 (기본값)
- **유한 차분 스텝**: 1e-2
- **그래디언트 계산 주기**: 5 에폭마다

### 물리 파라미터 범위
- **friction**: 0 ~ 2.0
- **car_mass**: > 500.0
- **wheel_mass**: > 10.0
- **kp_drive**: > 100.0
- **kv_drive**: > 10.0
- **kp_steer**: > 500.0
- **kv_steer**: > 50.0

## 🚀 사용 방법

### 기본 실행
```bash
python car_physics_learning_simple.py --csv car_motion_data.csv --urdf car.urdf --epochs 20 --cpu
```

### 옵션
- `--csv`: CSV 데이터 파일 경로 (기본값: `./car_motion_data.csv`)
- `--urdf`: URDF 파일 경로 (기본값: `./car.urdf`)
- `--epochs`: 학습 에폭 수 (기본값: 20)
- `--lr`: 학습률 (기본값: 1e-3)
- `--cpu`: CPU 사용 (기본값: GPU 시도)
- `--vis`: 시각화 표시 (기본값: False)

### 예시
```bash
# 기본 실행
python car_physics_learning_simple.py

# 시각화 포함
python car_physics_learning_simple.py --vis

# 더 많은 에폭으로 학습
python car_physics_learning_simple.py --epochs 50 --lr 5e-4

# CPU 사용
python car_physics_learning_simple.py --cpu
```

## 📤 출력

### 1. 학습 중 출력
- 에폭별 손실 값
- 현재 파라미터 값 (friction, kp_drive, kv_drive, kp_steer, kv_steer)
- 5 에폭마다 출력

### 2. 학습 완료 후 출력
- 최적 손실 값
- 최적 파라미터 값
- 저장된 파일:
  - `physics_model.pth`: 학습된 MLP 모델 가중치
  - `best_params.npy`: 최적 물리 파라미터

### 3. 출력 예시
```
🚀 MLP 학습 시작...
  타겟 데이터 크기: 250 프레임
Epoch [5/20], Loss: 0.123456
  Params: friction=1.234, kp_drive=500.0, kv_drive=50.0, kp_steer=1000.0, kv_steer=100.0
...
✅ 학습 완료! Best Loss: 0.012345
Best Params: [1.234 1200.0 20.0 500.0 50.0 1000.0 100.0]

최적 파라미터:
  friction: 1.234
  car_mass: 1200.000
  wheel_mass: 20.000
  kp_drive: 500.000
  kv_drive: 50.000
  kp_steer: 1000.000
  kv_steer: 100.000
```

## 🔧 기술적 세부사항

### 1. 유한 차분법 (Finite Difference Method)
- **이유**: Genesis 시뮬레이터가 미분 불가능하므로 그래디언트를 직접 계산할 수 없음
- **방법**: 중앙 차분법 사용
  ```
  gradient = (loss(params + ε) - loss(params - ε)) / (2ε)
  ```
- **최적화**: 5 에폭마다 한 번만 계산하여 비용 절감

### 2. 환경 변수 설정
- **`SETUPTOOLS_USE_DISTUTILS=stdlib`**: distutils hack 문제 해결
- **`torch._dynamo.config.suppress_errors = True`**: torch compile 관련 에러 억제

### 3. 시뮬레이션 래퍼
- **초기화**: 씬을 한 번만 초기화하여 비용 절감
- **리셋**: 시뮬레이션 실행 전 씬 리셋
- **상태 수집**: 각 스텝마다 차체 위치 및 속도 수집

### 4. 파라미터 범위 제한
- **활성화 함수 사용**:
  - `Sigmoid`: friction (0~2)
  - `Softplus`: mass, kp, kv (>0)
- **오프셋 추가**: 최소값 보장

## ⚠️ 주의사항

### 1. 계산 비용
- **유한 차분법**: 각 파라미터마다 2번의 시뮬레이션 실행 필요
- **비용 절감**: 5 에폭마다 한 번만 계산, 주요 파라미터만 계산
- **예상 시간**: 에폭당 수 분 ~ 수십 분 (데이터 크기 및 하드웨어에 따라)

### 2. 메모리 사용
- **시뮬레이션 상태**: 모든 스텝의 상태를 메모리에 저장
- **대용량 데이터**: 데이터 크기에 따라 메모리 사용량 증가

### 3. 수렴성
- **비선형 최적화**: 유한 차분법은 근사적이므로 수렴이 보장되지 않음
- **초기값 의존성**: 초기 파라미터에 따라 결과가 달라질 수 있음
- **로컬 최적값**: 전역 최적값을 찾지 못할 수 있음

### 4. Genesis 호환성
- **CPU/GPU**: Genesis는 CPU와 GPU 모두 지원
- **버전 호환성**: Genesis 버전에 따라 API가 다를 수 있음

## 🔍 문제 해결

### 1. distutils 에러
- **원인**: setuptools/triton 호환성 문제
- **해결**: 환경 변수 `SETUPTOOLS_USE_DISTUTILS=stdlib` 설정

### 2. pandas 모듈 없음
- **원인**: pandas가 설치되지 않음
- **해결**: `pip install pandas`

### 3. 시뮬레이션 에러
- **원인**: URDF 파일 문제 또는 파라미터 범위 초과
- **해결**: 
  - URDF 파일 확인
  - 파라미터 범위 확인
  - 초기 안정화 단계 증가

### 4. 수렴하지 않음
- **원인**: 학습률이 너무 크거나 작음
- **해결**: 
  - 학습률 조정 (`--lr` 옵션)
  - 에폭 수 증가 (`--epochs` 옵션)
  - 그래디언트 계산 주기 조정

## 📈 성능 최적화

### 1. 그래디언트 계산 최적화
- **현재**: 5 에폭마다 한 번만 계산
- **개선 방안**: 
  - Bayesian Optimization 사용
  - 미분 가능한 시뮬레이터 사용
  - 배치 처리

### 2. 시뮬레이션 최적화
- **현재**: 각 파라미터마다 전체 시뮬레이션 실행
- **개선 방안**:
  - 병렬 시뮬레이션
  - 시뮬레이션 스텝 수 감소
  - 샘플링 기반 최적화

### 3. 메모리 최적화
- **현재**: 모든 스텝의 상태 저장
- **개선 방안**:
  - 샘플링 기반 상태 저장
  - 스트리밍 방식 사용
  - 배치 처리

## 🔮 향후 개선 방안

### 1. 학습 방법 개선
- **Bayesian Optimization**: 더 효율적인 최적화
- **Reinforcement Learning**: 강화학습 기반 파라미터 탐색
- **Evolutionary Algorithms**: 진화 알고리즘 기반 최적화

### 2. 모델 개선
- **더 복잡한 모델**: LSTM, Transformer 등 시퀀스 모델 사용
- **시변 파라미터**: 시간에 따라 변하는 파라미터 학습
- **불확실성 추정**: 파라미터의 불확실성 추정

### 3. 데이터 개선
- **더 많은 데이터**: 다양한 주행 시나리오 데이터 수집
- **데이터 증강**: 데이터 증강 기법 적용
- **실시간 학습**: 실시간 데이터로 학습

## 📝 요약

`car_physics_learning_simple.py`는 MLP를 사용하여 Genesis 물리 시뮬레이터의 파라미터를 학습하는 시스템입니다. CSV 데이터를 사용하여 실제 차량의 움직임을 모방하는 물리 파라미터를 찾습니다. 유한 차분법을 사용하여 그래디언트를 계산하고, Adam 옵티마이저로 모델을 업데이트합니다.

### 주요 특징
- ✅ MLP 기반 파라미터 예측
- ✅ 유한 차분법으로 그래디언트 계산
- ✅ pandas를 사용한 빠른 데이터 로딩
- ✅ distutils 문제 해결
- ✅ 비용 절감을 위한 최적화

### 사용 시나리오
- 실제 차량 데이터로 시뮬레이터 파라미터 튜닝
- 물리 파라미터 최적화
- 시뮬레이터 검증 및 보정

## 📚 참고 자료

- **Genesis 문서**: [Genesis 공식 문서]
- **PyTorch 문서**: [PyTorch 공식 문서]
- **pandas 문서**: [pandas 공식 문서]
- **유한 차분법**: [Finite Difference Method]

## 🏁 결론

이 시스템은 실제 차량 데이터를 사용하여 물리 시뮬레이터의 파라미터를 학습하는 효과적인 방법을 제공합니다. MLP 모델을 사용하여 복잡한 물리 파라미터 관계를 학습하고, 유한 차분법을 통해 그래디언트를 계산하여 최적화합니다.

